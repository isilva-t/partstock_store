name: "online_catalog"

services:
  frontend-dev:
    profiles: ["development"]
    build:
      context: ./frontend
      target: development
    ports:
      - "4200:4200"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    networks:
      - catalog_network
    environment:
      - DOMAIN=${DOMAIN}
    restart: unless-stopped

  frontend-prod:
    profiles: ["production"]
    build:
      context: ./frontend
      target: production
    ports:
      - "${FRONT_PORT}:80"
      - "${FRONT_HTTPS}:443"
    volumes:
      - ./ssl:/etc/nginx/ssl:ro
    networks:
      - catalog_network
    environment:
      - DOMAIN=${DOMAIN}
    restart: unless-stopped

  backend:
    build:
      context: ./backend
      target: ${COMPOSE_PROFILES}
    container_name: backend
    volumes:
      - ./backend/src:/app/src
      - ./backend/pom.xml:/app/pom.sml:ro
      - maven-cache:/root/.m2
      - ${DATA_PATH}/photos:/app/photos
    depends_on:
      - mongodb
    networks:
      - catalog_network
    environment:
      - MONGO_URL=${MONGO_URL}
      - DATA_PATH=${DATA_PATH}
      - ORIGIN=${ORIGIN}
    restart: unless-stopped

  mongodb:
    image: mongo:7
    ports:
      - "27017:27017"
    volumes:
      - ${DATA_PATH}/mongo_data:/data/db
    networks:
      - catalog_network
    restart: unless-stopped

networks:
  catalog_network:
    driver: bridge

volumes:
  maven-cache:
